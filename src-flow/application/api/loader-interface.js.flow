import { FilterInterface } from './filter-interface.js.flow'
import type { PullRequestData } from './data/pullrequest.js.flow'
import { graphql } from '@octokit/graphql'

export interface LoaderInterface {
  constructor(
    githubClient: graphql,
    filters:Array<FilterInterface>
  ):void,
  execute(
    organization:string,
    repository:string,
    from: number,
    to: number,
  ):Promise<Array<PullRequestData>>
}
